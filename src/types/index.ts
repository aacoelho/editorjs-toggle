import { BlockToolData } from '@editorjs/editorjs';

/**
 * editorjs-toggle Tool's input and output data format
 */
export interface editorjs-toggleData extends BlockToolData {}

/**
 * editorjs-toggle Tool's configuration object that passed through the initial Editor config
 */
export interface editorjs-toggleConfig {}